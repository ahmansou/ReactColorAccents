import Layout from './components/Layout/Layout';
// import { colors } from './values/values';

const App = () => {
  return (
    <Layout
      // colors={colors.dark}
    />
  );
}

export default App;
